# Esempio verificato. Capitolo 32

`snip_32_contract.py` esegue il caso minimo usato nel testo di **Il ciclo di vita dei dati**. `test_32_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_32_contract.py
python -m unittest -v test_32_contract.py
```
