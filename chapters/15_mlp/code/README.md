# Esempio verificato. Capitolo 15

`snip_15_contract.py` esegue il caso minimo usato nel testo di **Dal percettrone alle reti multilayer**. `test_15_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_15_contract.py
python -m unittest -v test_15_contract.py
```
