# Esempio verificato. Capitolo 70

`snip_70_contract.py` esegue il caso minimo usato nel testo di **Multi-agent, browser, computer e code agents**. `test_70_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_70_contract.py
python -m unittest -v test_70_contract.py
```
