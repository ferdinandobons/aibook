# Esempio verificato. Capitolo 55

`snip_55_contract.py` esegue il caso minimo usato nel testo di **Fondamenti della multimodalità**. `test_55_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_55_contract.py
python -m unittest -v test_55_contract.py
```
