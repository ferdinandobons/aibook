# Esempio verificato. Capitolo 61

`snip_61_contract.py` esegue il caso minimo usato nel testo di **3D, spazio e rappresentazione delle scene**. `test_61_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_61_contract.py
python -m unittest -v test_61_contract.py
```
