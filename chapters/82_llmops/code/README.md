# Esempio verificato. Capitolo 82

`snip_82_contract.py` esegue il caso minimo usato nel testo di **LLMOps, edge, costo ed energia**. `test_82_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_82_contract.py
python -m unittest -v test_82_contract.py
```
