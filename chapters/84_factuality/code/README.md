# Esempio verificato. Capitolo 84

`snip_84_contract.py` esegue il caso minimo usato nel testo di **Fattualità, incertezza e affidabilità**. `test_84_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti e presenza del limite interpretativo.

```bash
python snip_84_contract.py
python -m unittest -v test_84_contract.py
```
