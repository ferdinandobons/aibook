# Esempio verificato. Capitolo 35

`snip_35_contract.py` esegue il caso minimo usato nel testo di **La ricetta di pretraining**. `test_35_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_35_contract.py
python -m unittest -v test_35_contract.py
```
