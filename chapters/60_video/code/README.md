# Esempio verificato. Capitolo 60

`snip_60_contract.py` esegue il caso minimo usato nel testo di **Generazione video**. `test_60_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_60_contract.py
python -m unittest -v test_60_contract.py
```
