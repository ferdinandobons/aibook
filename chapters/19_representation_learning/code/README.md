# Esempio verificato. Capitolo 19

`snip_19_contract.py` esegue il caso minimo usato nel testo di **Representation learning**. `test_19_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_19_contract.py
python -m unittest -v test_19_contract.py
```
