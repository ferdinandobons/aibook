# Esempio verificato. Capitolo 33

`snip_33_contract.py` esegue il caso minimo usato nel testo di **Dataset mixture, curriculum e dati sintetici**. `test_33_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti, forma del contratto e rifiuto dei casi non documentati.

```bash
python snip_33_contract.py
python -m unittest -v test_33_contract.py
```
