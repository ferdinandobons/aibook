# Esempio verificato. Capitolo 17

`snip_17_contract.py` esegue il caso minimo usato nel testo di **Convolutional network e apprendimento geometrico**. `test_17_contract.py` conserva l'output atteso, controlla determinismo, serializzazione, valori finiti e presenza del limite interpretativo.

```bash
python snip_17_contract.py
python -m unittest -v test_17_contract.py
```
