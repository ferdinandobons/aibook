# Testo alternativo

REPRESEN-01, Representation learning. Come si passa da «Che cosa rappresenta un vettore» a «Metric e contrastive learning» mantenendo osservabile un vettore prodotto per un compito successivo? La composizione latent geometry collega «Che cosa rappresenta un vettore», «Bottleneck e autoencoder», «Metric e contrastive learning». L'input è u = [1, 2, 0] e v = [2, 1, 0]; l'output è un vettore, una similarità o una predizione downstream. Il limite esplicito è: la geometria dipende da dati, obiettivo e normalizzazione.
