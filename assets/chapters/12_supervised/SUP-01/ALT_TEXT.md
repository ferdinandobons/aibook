# Alt text `SUP-01`

Workflow orizzontale su sfondo bianco. Un dataset di coppie input-target viene diviso in train, validation e test. Il train aggiorna i parametri con BCE e L2; la validation seleziona una soglia `0,30` in base ai costi di falsi negativi e falsi positivi; il test applica modello e soglia ormai fissati e riporta accuracy `0,900`, recall `0,913` e costo `13`. Il footer avverte che consultare il test durante il tuning restringe il claim.

Stato: verificato su `candidate-v1.png`.
