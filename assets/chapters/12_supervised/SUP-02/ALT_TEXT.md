# Alt text `SUP-02`

Confronto di due matrici di confusione. A sinistra la soglia `0,30` produce TP `21`, FN `2`, FP `3`, TN `24`, accuracy `0,900` e costo pesato `13`. A destra la soglia `0,50` produce TP `19`, FN `4`, FP `1`, TN `26`, la stessa accuracy e costo `21`. Il pannello centrale evidenzia che le predizioni corrette sono `45` su `50`, ma gli errori cambiano. La fascia inferiore separa tracking disponibile e mancante.

Stato: verificato su `candidate-v1.png`.
