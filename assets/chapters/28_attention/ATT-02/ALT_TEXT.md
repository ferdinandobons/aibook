# Alt text ATT-02

Diagramma in quattro fasi. La query `[1,0]` viene confrontata con tre key `[1,0]`, `[0,1]` e `[1,1]`; i prodotti scalari `[1,0,1]` vengono divisi per `sqrt(2)`, producendo `[0,707,0,0,707]`. La softmax produce i pesi `[0,401,0,198,0,401]`, che combinano le value `[1,0]`, `[0,1]` e `[1,1]` nell'output `[0,802,0,599]`.
