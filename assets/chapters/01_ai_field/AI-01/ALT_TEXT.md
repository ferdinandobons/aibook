# Alt text per `AI-01`

Diagramma orizzontale su sfondo bianco. In alto al centro compare il caso guida `Il pacco non è arrivato`. Tre frecce separate conducono a pannelli paralleli e non gerarchici. Il pannello blu `Meccanismo` contiene regole esplicite, modello appreso dai dati e sistema ibrido. Il pannello viola `Obiettivo` contiene classificare o predire, decidere e generare contenuto. Il pannello verde `Ampiezza` contiene specialistico, base adattabile e generalista. In basso, un box ambra afferma che i tre aspetti possono combinarsi e che nessuno determina automaticamente gli altri.

Stato: verificato su `candidate-v1.png`.
