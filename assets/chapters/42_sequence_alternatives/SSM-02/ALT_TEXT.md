# Testo alternativo

SSM-02, State-space model, recurrence e long convolution. Quale controllo collega «Hyena e long convolution» a «RWKV, RetNet, xLSTM e Griffin» senza superare il limite dichiarato? La composizione sequence stack collega «Hyena e long convolution», «RWKV, RetNet, xLSTM e Griffin». L'input è x_t, stato s_t e matrici A, B, C; l'output è stato e uscita per ogni posizione. Il limite esplicito è: stabilità e discretizzazione fanno parte dell'implementazione.
